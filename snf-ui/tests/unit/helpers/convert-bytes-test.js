import {
  convertBytes
} from 'snf-ui/helpers/convert-bytes';

module('ConvertBytesHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = convertBytes(42);
  ok(result);
});
