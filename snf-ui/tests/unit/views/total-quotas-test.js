import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:total-quotas', 'TotalQuotasView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
