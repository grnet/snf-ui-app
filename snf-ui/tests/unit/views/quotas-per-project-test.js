import {
  moduleFor,
  test
} from 'ember-qunit';

moduleFor('view:quotas-per-project', 'QuotasPerProjectView');

// Replace this with your real tests.
test('it exists', function() {
  var view = this.subject();
  ok(view);
});
