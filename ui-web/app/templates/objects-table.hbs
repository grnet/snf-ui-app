<thead>
    <tr>
        {{view "th-sortable" field="stripped_name" title="name"}}
        <th>&nbsp;</th>
        {{view "th-sortable" field="last_modified" title="modified"}}
        {{view "th-sortable" field="type"}}
        {{view "th-sortable" field="size"}}
        <th class="download">&nbsp;</th>
        <th>&nbsp;</th>
    </tr>
</thead>
<tbody>
{{#each}}
    {{view 'object'}}
{{else}}
    <tr><td rowspan="5">No objects in this directory</td></tr>
{{/each}}
</tbody>
