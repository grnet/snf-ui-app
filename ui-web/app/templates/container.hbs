{{outlet}}
<h4>Container:{{id}}</h4>
<ul>
{{#each o in objects}}
    <li>
    {{#if o.is_dir}}
        {{#link-to 'container' name o.name }}
            {{fa-icon "fa-folder-open"}}
            {{o.name}}
        {{/link-to}}
    {{else}}
        {{fa-icon "fa-file-o"}}
        {{o.name}}
    {{/if}}
    </li>
{{/each}}
</ul>
