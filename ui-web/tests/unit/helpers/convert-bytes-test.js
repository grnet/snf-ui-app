import {
  convertBytes
} from 'ui-web/helpers/convert-bytes';

module('ConvertBytesHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = convertBytes(42);
  ok(result);
});
